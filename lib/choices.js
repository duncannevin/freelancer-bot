/**
* choices are any choices that update userState.stage
* it is important to endure any commands that happen
* withing the subStage state are different than
* these because these commands are recognized by
* index.js
*/

export default {
  one: ['Browse jobs', 'Post a job'],
}